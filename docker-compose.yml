name: memory-mcp

services:
  memory-mcp:
    build:
      context: .
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      MEMORY_MCP_ENV_FILE: /app/.env
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    stdin_open: true
    tty: true

  gateway:
    build:
      context: .
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      MEMORY_MCP_ENV_FILE: /app/.env
    command: ["memory-mcp", "--transport", "streamable-http", "--host", "0.0.0.0", "--port", "3000"]
    ports:
      - "${GATEWAY_BIND_HOST:-0.0.0.0}:${GATEWAY_PORT:-3000}:3000"
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  distiller:
    build:
      context: .
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      MEMORY_MCP_ENV_FILE: /app/.env
      LOG_LEVEL: INFO
    command: ["python", "-m", "memory_mcp.distiller.runner"]
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-memory_mcp}
      POSTGRES_USER: ${POSTGRES_USER:-memory_mcp}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "${POSTGRES_BIND_HOST:-127.0.0.1}:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - "${PGDATA_HOST_PATH}:/var/lib/postgresql/data"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
