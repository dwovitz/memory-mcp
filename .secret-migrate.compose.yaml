# Generated by secret-migrate; contains variable references only.
services:
  "distiller":
    environment:
      "POSTGRES_PASSWORD": "${MEMORY_MCP__POSTGRES_PASSWORD}"
  "gateway":
    environment:
      "POSTGRES_PASSWORD": "${MEMORY_MCP__POSTGRES_PASSWORD}"
  "memory-mcp":
    environment:
      "POSTGRES_PASSWORD": "${MEMORY_MCP__POSTGRES_PASSWORD}"
