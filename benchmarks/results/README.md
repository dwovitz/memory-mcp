# Benchmark Results

Store completed benchmark outputs here as one Markdown file per case and
variant. Keep the raw `BENCHMARK_RESULT` block intact so baseline and memory
runs can be compared without reformatting.

Recommended filename:

```text
<case-id>-<variant>.md
```
